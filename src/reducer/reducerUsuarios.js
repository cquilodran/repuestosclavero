export function reducerUsuarios(state, action) {

  switch (action.type) {
    case "ACTUALIZA_LISTA_USUARIOS":

      return state
    case "BUSCANDO_USUARIOS":
      return state

    default:
      return state
  }
}